para o cli interativo, vamos usar terminal-kit

para o web scrapping vamos usar playwright

para mostrar o qrcode no terminal, podemos usar qrcode-terminal

para mostrar o codigo de barras no terminal, podemos usar terminal-kit

para a barra de loading podemos usar o terminal-kit, pode ser feito em um helper para ser reutilizado em todos os providers ou pode ser feito no proprio contrato ou na class

para requisições vamos usar o axios
